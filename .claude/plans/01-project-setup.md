# Project Setup

Scope: Laravel 12 + PHP 8.3 install, MySQL connection, Laravel Boost, base Blade layout with RTL/LTR (Arabic + English) support, i18n string catalog.

Status: ✅ Done.

- [x] Laravel 12 + PHP 8.3
- [x] MySQL connection
- [x] Laravel Boost
- [x] Base Blade layout with RTL/LTR (Arabic + English) support
- [x] Bilingual UI string catalog (`lang/ar.json`, `lang/en.json`) covering the whole app

Note: the asset pipeline actually used is Tailwind CSS + AlpineJS, not the Bootstrap 5 + SCSS
originally scoped above — see `.claude/CLAUDE.md` for the current tech stack. Scope line kept
for history; treat the CLAUDE.md stack list as authoritative.
