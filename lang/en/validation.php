<?php

declare(strict_types=1);

return [

    // Merged on top of Laravel's built-in defaults (see vendor/laravel/framework's
    // internal lang/en/validation.php) — only keys not covered by the framework
    // belong here, since the merge is per-key, not a full file replace.

    'phone' => 'The :attribute must be a valid phone number.',

];
